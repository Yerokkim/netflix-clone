import 'package:flutter/material.dart';

class SelectUserPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(centerTitle: true, title: Text('Who is watching?')),
      body: Column(children: []),
    );
  }
}
